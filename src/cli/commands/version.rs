use crate::error::Result;

pub fn handle() -> Result<()> {
    println!("cnpm 0.1.0 - Node.js package manager written in Rust");
    println!("cnode 0.1.0 - Rust FFI bindings for Node.js");
    Ok(())
}
